(ns com.phronemophobic.reveal-treemap-test
  (:require [clojure.test :refer :all]
            [com.phronemophobic.reveal-treemap :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
